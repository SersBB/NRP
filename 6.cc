#include <iostream>
using namespace std;

int main()
{
  string a;
  cin>>a;
  if(a=="ponedeljek"||a=="torek")
    cout<<"Mateja"<<endl;
  if(a=="sreda")
    cout<<"Irena"<<endl;
  if(a=="cetrtek")
    cout<<"Tadeja"<<endl;
  if(a=="petek")
    cout<<"Jana"<<endl;
  return 0;
}
